# 首先确定表数据的数据结构
# 根据表数据的数据结构确定索引的方式
# the struct of data is img_name, feature_num, feature_num, x, y
# 在构建索引的过程中，首先需要选定进行索引的属性。

# from binarytree import tree, pprint, inspect

# 输出应该包含几个部分，首先是 记录数目，然后是构建索引的时间，以及在一定数量级下插入数据的时间。

import time
import csv


class BPlustreeIndex():
    def __init__(self):
        pass

    def insert_point(self, ):
        # 用于在 B+ 树 中插入数据
        def if_out_of_range(self):
            pass

    def delete_point(self):
        pass

    def search_point(self):
        pass


if __name__ == "__main__":
    # 首先，所有的数据都是存储在 csv 文件中的
    # 然后 对比索引的构建时间和查询时间的对比

    pass
